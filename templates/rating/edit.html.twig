{% include 'default/_form.html.twig' with {
    page_title: 'title.rating_edit'|trans({'%id%': rating.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'rating_index',
} %}