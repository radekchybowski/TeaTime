{% include 'default/_form.html.twig' with {
    page_title: 'title.rating_delete'|trans({'%id%': rating.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'rating_index',
} %}
